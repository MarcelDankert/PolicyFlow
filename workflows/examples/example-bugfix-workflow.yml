workflow:
  id: example-bugfix-workflow
  type: bugfix
context:
  workflow_file: workflows/examples/example-bugfix-workflow.yml
  bug_summary: Fix invalid payload handling in a parser
  scope:
    - parser validation
  non_goals:
    - no contract shape changes
  affected_modules:
    - runtime layer
  risk_level: MEDIUM

governance:
  required_reviews:
    - architecture-agent
    - review-agent
    - qa-agent
  human_approval_required: false
