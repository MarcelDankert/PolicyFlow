workflow:
  id: loop-governance-iteration-exceeded
  type: feature

context:
  workflow_file: tests/fixtures/loop-governance-iteration-exceeded.yml
  risk_level: MEDIUM
  confidence:
    planning: Loop governance fixture declares an exceeded iteration limit.
    implementation: Implementation is intentionally invalid for validator tests.
    tests: Tests assert current_iteration cannot exceed max_iterations.
    residual_uncertainty: None; this fixture is expected to fail validation.

governance:
  required_reviews:
    - architecture-agent
    - review-agent
    - qa-agent
  human_approval_required: false
  escalation_required: false
  protected_areas_touched:
    - none

execution:
  mode: strict
  phases:
    - phase: planning
      state: completed
    - phase: architecture-check
      state: completed
    - phase: implementation
      state: pending
    - phase: review
      state: pending
    - phase: qa
      state: pending

evidence:
  planning:
    summary: Invalid exceeded current_iteration fixture.
    scope_locked:
      - loop governance max iteration validation
    non_goals_locked:
      - no loop execution
    risk_rationale: MEDIUM risk fixture for Loop Governance validation.
  architecture-check:
    decision: Exceeded current_iteration should fail as a compliance failure.
    constraints:
      - no runtime behavior
    approval_path: architecture-agent review

loop_governance:
  loops:
    - id: review-feedback
      source_phase: review
      target_phase: implementation
      allowed_feedback_sources:
        - review-findings
      max_iterations: 3
      current_iteration: 4
      status: active
      stop_conditions:
        - id: review-findings-resolved
          description: All blocking review findings are resolved.
      escalation_conditions:
        - id: iteration-limit-reached
          trigger: max_iterations_exceeded
          escalate_to: human-arbitration
      evidence_refs:
        - evidence.review

contracts:
  planning:
    owner_agent: planning-agent
    issue_brief: Invalid fixture for exceeded loop iteration validation.
    acceptance_criteria:
      - current_iteration cannot exceed max_iterations
    approved_scope:
      - loop governance max iteration validation
    non_goals:
      - no loop execution
    initial_risk_level: MEDIUM
    protected_areas_touched:
      - none
    confidence_summary: Fixture is intentionally invalid.
    escalation_flags:
      - none
  architecture-check:
    owner_agent: architecture-agent
    architecture_assessment: Exceeded current_iteration is represented as a compliance failure.
    approved_scope:
      - loop governance max iteration validation
    module_boundaries:
      - validator fixtures
    contract_impact: None.
    risk_review_decision: MEDIUM risk remains appropriate.
    required_reviews:
      - architecture-agent
      - review-agent
      - qa-agent
    implementation_constraints:
      - no runtime behavior

overrides: []

runtime:
  status: idle
  current_phase: implementation
  active_agent: senior-dev-agent
  last_transition: architecture-check completed

handoffs: []
