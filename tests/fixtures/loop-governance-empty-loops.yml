workflow:
  id: loop-governance-empty-loops
  type: feature

context:
  workflow_file: tests/fixtures/loop-governance-empty-loops.yml
  risk_level: MEDIUM
  confidence:
    planning: Loop governance fixture declares an invalid empty loop list.
    implementation: Implementation is intentionally invalid for validator tests.
    tests: Tests assert empty loop lists fail model validation.
    residual_uncertainty: None; this fixture is expected to fail validation.

governance:
  required_reviews:
    - architecture-agent
    - review-agent
    - qa-agent
  human_approval_required: false
  escalation_required: false
  protected_areas_touched:
    - none

execution:
  mode: strict
  phases:
    - phase: planning
      state: completed
    - phase: architecture-check
      state: completed
    - phase: implementation
      state: pending
    - phase: review
      state: pending
    - phase: qa
      state: pending

evidence:
  planning:
    summary: Invalid empty loop_governance fixture.
    scope_locked:
      - loop governance model
    non_goals_locked:
      - no loop execution
    risk_rationale: MEDIUM risk fixture for Loop Governance model validation.
  architecture-check:
    decision: Empty loop lists should fail model validation.
    constraints:
      - no runtime behavior
    approval_path: architecture-agent review

loop_governance:
  loops: []

contracts:
  planning:
    owner_agent: planning-agent
    issue_brief: Invalid fixture for empty loop list validation.
    acceptance_criteria:
      - empty loops fail validation
    approved_scope:
      - loop governance model
    non_goals:
      - no loop execution
    initial_risk_level: MEDIUM
    protected_areas_touched:
      - none
    confidence_summary: Fixture is intentionally invalid.
    escalation_flags:
      - none
  architecture-check:
    owner_agent: architecture-agent
    architecture_assessment: Empty loop list is represented as invalid metadata.
    approved_scope:
      - loop governance model
    module_boundaries:
      - validator fixtures
    contract_impact: None.
    risk_review_decision: MEDIUM risk remains appropriate.
    required_reviews:
      - architecture-agent
      - review-agent
      - qa-agent
    implementation_constraints:
      - no runtime behavior

overrides: []

runtime:
  status: idle
  current_phase: implementation
  active_agent: senior-dev-agent
  last_transition: architecture-check completed

handoffs: []
