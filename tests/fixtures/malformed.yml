workflow:
  id: malformed
  type: feature
context:
  workflow_file: workflows/examples/malformed.yml
  risk_level: LOW
  confidence:
    planning: Planning confidence is documented before implementation.
    implementation: Implementation confidence is bounded by declared scope and constraints.
    tests: Test confidence is based on declared validation evidence.
    residual_uncertainty: Residual uncertainty is tracked in review and QA.governance:
  required_reviews:
    - review-agent
  human_approval_required: [false

