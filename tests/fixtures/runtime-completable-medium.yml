workflow:
  id: runtime-completable-medium
  type: feature

context:
  workflow_file: workflows/examples/runtime-completable-medium.yml
  risk_level: MEDIUM
  confidence:
    planning: Planning confidence is documented before implementation.
    implementation: Implementation confidence is bounded by declared scope and constraints.
    tests: Test confidence is based on declared validation evidence.
    residual_uncertainty: Residual uncertainty is tracked in review and QA.
governance:
  required_reviews:
    - architecture-agent
    - review-agent
    - qa-agent
  human_approval_required: false

execution:
  mode: strict
  phases:
    - phase: planning
      state: completed
    - phase: architecture-check
      state: completed
    - phase: implementation
      state: in_progress
    - phase: review
      state: pending
    - phase: qa
      state: pending

evidence:
  planning:
    summary: Medium-risk scope and non-goals locked before coding.
    scope_locked:
      - parser validation
    non_goals_locked:
      - no contract changes
    risk_rationale: MEDIUM risk due to cross-check needs.
  architecture-check:
    decision: Architecture check completed before implementation.
    constraints:
      - keep implementation bounded
    approval_path: architecture-agent review

contracts:
  planning:
    owner_agent: planning-agent
    issue_brief: Lock parser validation scope before coding.
    acceptance_criteria:
      - parser validation remains bounded
    approved_scope:
      - parser validation
    non_goals:
      - no contract changes
    initial_risk_level: MEDIUM
    protected_areas_touched:
      - none
    confidence_summary: Medium-risk scope is stable enough for architecture review.
    escalation_flags:
      - none
  architecture-check:
    owner_agent: architecture-agent
    architecture_assessment: Architecture scope remains inside parser validation.
    approved_scope:
      - parser validation
    module_boundaries:
      - parser module only
    contract_impact: No contract changes required.
    risk_review_decision: MEDIUM risk path stays in place.
    required_reviews:
      - architecture-agent
      - review-agent
      - qa-agent
    implementation_constraints:
      - keep implementation bounded
  implementation:
    owner_agent: senior-dev-agent
    implementation_summary: Implemented the bounded parser validation change.
    changed_files:
      - policyflow/validator.py
    test_summary: Validator checks passed for the implementation slice.
    docs_updates:
      - README.md
    known_limitations:
      - no contract changes
    unresolved_questions:
      - none

runtime:
  status: in_progress
  current_phase: implementation
  active_agent: senior-dev-agent
  last_transition: implementation started

handoffs: []

