workflow:
  id: loop-governance-missing-escalation-conditions
  type: feature

context:
  workflow_file: tests/fixtures/loop-governance-missing-escalation-conditions.yml
  risk_level: MEDIUM
  confidence:
    planning: Loop governance fixture declares an invalid empty escalation condition list.
    implementation: Implementation is intentionally invalid for validator tests.
    tests: Tests assert empty escalation_conditions fail as declaration errors.
    residual_uncertainty: None; this fixture is expected to fail validation.

governance:
  required_reviews:
    - architecture-agent
    - review-agent
    - qa-agent
  human_approval_required: false
  escalation_required: false
  protected_areas_touched:
    - none

execution:
  mode: strict
  phases:
    - phase: planning
      state: completed
    - phase: architecture-check
      state: completed
    - phase: implementation
      state: pending
    - phase: review
      state: pending
    - phase: qa
      state: pending

evidence:
  planning:
    summary: Invalid empty escalation_conditions fixture.
    scope_locked:
      - loop governance escalation validation
    non_goals_locked:
      - no loop execution
    risk_rationale: MEDIUM risk fixture for Loop Governance validation.
  architecture-check:
    decision: Empty escalation_conditions should fail as a declaration error.
    constraints:
      - no runtime behavior
    approval_path: architecture-agent review

loop_governance:
  loops:
    - id: review-feedback
      source_phase: review
      target_phase: implementation
      allowed_feedback_sources:
        - review-findings
      max_iterations: 3
      current_iteration: 1
      status: active
      stop_conditions:
        - id: review-findings-resolved
          description: All blocking review findings are resolved.
      escalation_conditions: []
      evidence_refs:
        - evidence.review

contracts:
  planning:
    owner_agent: planning-agent
    issue_brief: Invalid fixture for loop escalation condition validation.
    acceptance_criteria:
      - empty escalation_conditions fail validation
    approved_scope:
      - loop governance escalation validation
    non_goals:
      - no loop execution
    initial_risk_level: MEDIUM
    protected_areas_touched:
      - none
    confidence_summary: Fixture is intentionally invalid.
    escalation_flags:
      - none
  architecture-check:
    owner_agent: architecture-agent
    architecture_assessment: Empty escalation_conditions are represented as invalid metadata.
    approved_scope:
      - loop governance escalation validation
    module_boundaries:
      - validator fixtures
    contract_impact: None.
    risk_review_decision: MEDIUM risk remains appropriate.
    required_reviews:
      - architecture-agent
      - review-agent
      - qa-agent
    implementation_constraints:
      - no runtime behavior

overrides: []

runtime:
  status: idle
  current_phase: implementation
  active_agent: senior-dev-agent
  last_transition: architecture-check completed

handoffs: []
