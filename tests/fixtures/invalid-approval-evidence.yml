workflow:
  id: invalid-approval-evidence
  type: architecture-change

context:
  workflow_file: workflows/examples/invalid-approval-evidence.yml
  risk_level: HIGH

governance:
  required_reviews:
    - architecture-agent
    - review-agent
    - qa-agent
    - human approval
  human_approval_required: true
  escalation_required: true
  protected_areas_touched:
    - database schema
  approval_evidence:
    - approved in architecture review

execution:
  mode: strict
  phases:
    - phase: planning
      state: completed
    - phase: architecture-check
      state: completed
    - phase: implementation
      state: pending
    - phase: review
      state: pending
    - phase: qa
      state: pending
    - phase: approval
      state: completed

evidence:
  planning:
    summary: High-risk scope locked before coding.
    scope_locked:
      - schema change planning
    non_goals_locked:
      - no runtime implementation
    risk_rationale: Protected area touched.
  architecture-check:
    decision: High-risk architecture review completed.
    constraints:
      - preserve approval path
    approval_path: human approval required
  approval:
    approved_by: architecture board
    reference: ADR-42
